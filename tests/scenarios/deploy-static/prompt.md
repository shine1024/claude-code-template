다음 4개 파일을 순서대로 정확히 수정해. 다른 어떤 작업도 하지 마.

1. src/main/resources/static/js/app.js 의 '[reservation] app initialized' 를 '[reservation] verified' 로 변경
2. src/main/resources/static/css/style.css 의 'color: #222;' 를 'color: #111;' 로 변경
3. src/main/resources/templates/sample.hbs 의 '<title>{{title}}</title>' 를 '<title>{{title}} (verified)</title>' 로 변경
4. build.gradle 의 group = 'com.unipost.uniflow' 를 group = 'com.unipost.uniflow.test' 로 변경

수정 후 '4개 파일 수정 완료' 만 응답.
